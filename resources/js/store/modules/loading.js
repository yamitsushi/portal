const state = {
  isLoading: true
}

const getters = {}

const actions = {}

const mutations = {
  isLoading (state, status) {
    state.isLoading = status
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
